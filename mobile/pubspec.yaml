name: mobile
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  go_router: ^17.3.0
  dio: ^5.11.0
  flutter_secure_storage: ^9.2.2
  path_provider: ^2.1.6
  pdfx: ^2.9.2
  package_info_plus: ^8.1.2
  image_picker: ^1.1.2
  shared_preferences: ^2.5.3
  local_auth: ^2.3.0
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  file_picker: 11.0.3
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  mocktail: ^1.0.4
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.6
  # Already resolved transitively via url_launcher (see pubspec.lock) —
  # declared explicitly here only so test code can substitute a fake
  # UrlLauncherPlatform (Mobile Fix #8's contact_deendoon_card_test.dart),
  # the officially-supported way to test url_launcher calls without
  # touching production code. Not a new package in the dependency graph.
  url_launcher_platform_interface: ^2.3.2

# Generates mipmap-*/ic_launcher.png from the existing approved Deendoon
# logo (assets/deendoon_logo.png) — run `dart run flutter_launcher_icons`
# after changing this. Adaptive icon background uses AppColors.background
# (#121417), the same brand dark surface used throughout the app, not an
# invented color.
flutter_launcher_icons:
  android: true
  image_path: "assets/deendoon_logo.png"
  adaptive_icon_background: "#121417"
  adaptive_icon_foreground: "assets/deendoon_logo.png"

# Generates the native splash screen (both launch_background.xml variants)
# from the same approved logo, on the same brand background color, so the
# native splash matches the in-app dark theme instead of Flutter's default
# white flash. Run `dart run flutter_native_splash:create` after changing
# this.
flutter_native_splash:
  color: "#121417"
  image: "assets/deendoon_logo.png"
  android: true
  ios: false
  web: false

# jni 1.0.1 (pulled transitively via flutter_secure_storage_windows ->
# path_provider -> path_provider_android) ships an Android Gradle script
# that unconditionally references a `kotlin {}` DSL block assuming AGP 9+
# auto-applies Kotlin support; with this project's actual AGP 9.0.1 +
# Kotlin 2.3.20 setup that extension isn't registered, so the build fails
# with "Could not find method kotlin()". jni 1.0.0 predates that Kotlin
# block entirely and builds cleanly — pinned here as a local project
# workaround for the upstream package bug, not an application change.
dependency_overrides:
  jni: 1.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # Generates AppLocalizations from lib/l10n/*.arb per l10n.yaml.
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  assets:
    - assets/deendoon_logo.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
