<?xml version="1.0" encoding="utf-8"?>
<!--
  Local development only. Android blocks cleartext (non-HTTPS) traffic by
  default for apps targeting API 28+, which silently drops every request
  to the Laravel dev server (plain http://10.0.2.2:8000). This scopes the
  cleartext exception to the emulator's host-loopback alias and direct
  localhost/adb-reverse access only — not a blanket cleartext allowance,
  and it has no effect on any real (HTTPS) backend.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
